# 修复 Vercel 404 错误

## 🔍 问题诊断

访问 https://11word.vercel.app/ 返回404，说明Vercel没有正确识别Next.js应用的位置。

## ✅ 解决步骤

### 步骤1：在Vercel Dashboard中设置Root Directory

这是**最关键**的一步！

1. 访问项目设置：https://vercel.com/wangpeng10170414-1653s-projects/11word/settings
2. 找到 **"Root Directory"** 部分（在General设置中）
3. 点击 **"Edit"** 按钮
4. 在输入框中输入：`web-admin`
5. 点击 **"Save"** 保存

### 步骤2：提交最新代码

在Git Bash中执行：

```bash
cd /e/trae/1单词

# 删除根目录的vercel.json（如果存在）
rm -f vercel.json

# 添加所有更改
git add .

# 提交
git commit -m "Fix 404: Add health check API and update vercel config"

# 推送
git push origin main
```

### 步骤3：触发重新部署

代码推送后，Vercel会自动重新部署。

或者手动触发：
1. 访问：https://vercel.com/wangpeng10170414-1653s-projects/11word/deployments
2. 点击最新部署右侧的三个点 **"..."**
3. 选择 **"Redeploy"**
4. 勾选 **"Use existing Build Cache"**（可选）
5. 点击 **"Redeploy"**

### 步骤4：等待部署完成

部署通常需要1-3分钟。完成后：

1. 访问首页：https://11word.vercel.app/
2. 测试健康检查：https://11word.vercel.app/api/health
3. 测试登录API：https://11word.vercel.app/api/auth/login

## 🧪 验证部署成功

### 测试1：访问首页
```
https://11word.vercel.app/
```
应该看到：**智能词汇复习助手 - 教师管理后台** 页面

### 测试2：健康检查API
```
https://11word.vercel.app/api/health
```
应该返回：
```json
{
  "success": true,
  "data": {
    "status": "ok",
    "message": "API is running",
    "timestamp": "2024-11-03T...",
    "environment": "production"
  }
}
```

### 测试3：登录API
```
https://11word.vercel.app/api/auth/login
```
应该返回错误（这是正常的）：
```json
{
  "success": false,
  "error": "密码不能为空"
}
```

## 🔧 如果还是404

### 检查部署日志

1. 访问：https://vercel.com/wangpeng10170414-1653s-projects/11word/deployments
2. 点击最新的部署
3. 查看 **"Building"** 和 **"Deployment"** 日志
4. 查找错误信息

### 常见问题

#### Q: Root Directory设置后还是404
**A:** 
1. 确保设置保存成功
2. 必须重新部署才能生效
3. 检查是否有多个vercel.json文件

#### Q: 部署成功但页面空白
**A:** 
1. 打开浏览器开发者工具（F12）
2. 查看Console是否有错误
3. 查看Network标签的请求状态

#### Q: 显示"Internal Server Error"
**A:** 
1. 检查环境变量是否正确配置
2. 查看Vercel部署日志中的错误
3. 确认DATABASE_URL已设置

## 📸 正确的配置截图参考

Root Directory应该显示为：
```
Root Directory: web-admin
```

Framework Preset应该显示为：
```
Framework Preset: Next.js
```

Build Command应该显示为：
```
Build Command: npm run build
```

Output Directory应该显示为：
```
Output Directory: .next
```

Install Command应该显示为：
```
Install Command: npm install
```

## 🆘 仍然有问题？

请提供以下信息：
1. Vercel部署日志的截图
2. Root Directory的设置截图
3. 浏览器Console的错误信息

---

**预计修复时间**：5-10分钟（主要是等待部署）
