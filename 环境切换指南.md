# 环境切换指南

## 🎯 快速切换环境

### 小程序环境切换

#### 方法：修改环境配置文件

**文件位置**: `wechat-miniapp/config/env.js`

```javascript
// 修改这一行即可切换环境
const ENV = 'production' // 'development' | 'preview' | 'production'
```

#### 环境说明

| 环境 | ENV值 | API地址 | 用途 |
|------|-------|---------|------|
| 本地开发 | `development` | http://localhost:3000/api | 本地调试 |
| Vercel预览 | `preview` | https://11word.vercel.app/api | 功能测试 |
| 阿里云生产 | `production` | http://47.92.96.143:3000/api | 正式使用 |

#### 切换步骤

1. 打开 `wechat-miniapp/config/env.js`
2. 修改 `const ENV = 'xxx'`
3. 保存文件
4. 重新编译小程序（微信开发者工具会自动重新编译）

---

## 🌐 后端环境管理

### 本地开发环境

#### 配置文件: `.env.local`
```bash
DATABASE_URL="file:./dev.db"
NODE_ENV="development"
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
```

#### 启动命令
```bash
cd web-admin
npm run dev
```

---

### Vercel预览环境

#### 自动部署
- 推送到 `develop` 分支自动部署
- 无需手动配置环境变量（Vercel自动注入）

#### 手动配置（如需要）
在Vercel Dashboard中配置：
- `DATABASE_URL`: 自动注入
- `BLOB_READ_WRITE_TOKEN`: 手动配置
- `NODE_ENV`: preview

---

### 阿里云生产环境

#### 配置文件: `.env.server`（服务器上）
```bash
DATABASE_URL="postgresql://word_user:word_pass_2024@postgres:5432/word_app"
BLOB_READ_WRITE_TOKEN="your_production_token"
NODE_ENV="production"
```

#### 部署命令
```bash
# 方法1: 使用部署脚本
bash deploy-production.sh

# 方法2: 使用原有脚本
bash deploy.sh
```

---

## 📋 环境检查清单

### 部署前检查

#### 1. 代码检查
- [ ] 所有更改已提交
- [ ] 代码已推送到远程仓库
- [ ] 分支正确（main用于生产）

#### 2. 环境变量检查
- [ ] 数据库连接配置正确
- [ ] API密钥配置正确
- [ ] 环境标识正确

#### 3. 小程序配置检查
- [ ] API地址配置正确
- [ ] 环境切换到目标环境

#### 4. 功能测试
- [ ] 登录功能正常
- [ ] API接口正常
- [ ] 数据库连接正常

---

## 🔄 常见场景

### 场景1: 本地开发新功能

```bash
# 1. 创建功能分支
git checkout -b feature/new-feature

# 2. 配置本地环境
# 确保.env.local存在且配置正确

# 3. 启动开发服务器
cd web-admin
npm run dev

# 4. 配置小程序环境
# 修改 wechat-miniapp/config/env.js
# const ENV = 'development'

# 5. 开发和测试
# ...
```

### 场景2: 部署到Vercel测试

```bash
# 1. 提交代码
git add .
git commit -m "feat: 新功能"

# 2. 推送到develop分支
git push origin feature/new-feature

# 3. 创建PR合并到develop
# 在GitHub上操作

# 4. Vercel自动部署
# 等待部署完成

# 5. 配置小程序测试
# 修改 wechat-miniapp/config/env.js
# const ENV = 'preview'

# 6. 测试功能
# ...
```

### 场景3: 发布到生产环境

```bash
# 1. 合并到main分支
git checkout main
git merge develop
git push origin main

# 2. 部署到阿里云
bash deploy-production.sh

# 3. 配置小程序生产环境
# 修改 wechat-miniapp/config/env.js
# const ENV = 'production'

# 4. 验证部署
curl http://47.92.96.143:3000/api/health

# 5. 全面测试
# ...
```

---

## ⚠️ 注意事项

### 1. 环境隔离
- ✅ 不同环境使用不同数据库
- ✅ 测试数据不要污染生产环境
- ✅ 生产环境配置不提交到Git

### 2. 小程序配置
- ⚠️ 每次切换环境后需要重新编译
- ⚠️ 发布前确认环境配置正确
- ⚠️ HTTP地址需要在开发者工具中开启"不校验合法域名"

### 3. 数据库迁移
- ⚠️ 生产环境数据库变更需谨慎
- ⚠️ 先在测试环境验证迁移脚本
- ⚠️ 生产环境迁移前备份数据

### 4. 部署安全
- ⚠️ 生产部署需要二次确认
- ⚠️ 部署后验证服务状态
- ⚠️ 保留部署日志

---

## 🆘 故障排查

### 问题1: 小程序无法连接API

**检查项**:
1. 环境配置是否正确
2. API地址是否可访问
3. 开发者工具是否开启"不校验合法域名"
4. 服务器是否正常运行

**解决方法**:
```bash
# 1. 检查服务器状态
ssh root@47.92.96.143 "docker ps"

# 2. 查看应用日志
ssh root@47.92.96.143 "cd /root/word-app && docker compose logs app"

# 3. 测试API连接
curl http://47.92.96.143:3000/api/health
```

### 问题2: 部署后功能异常

**检查项**:
1. 环境变量是否配置正确
2. 数据库迁移是否执行
3. 代码是否完全上传
4. 容器是否正常启动

**解决方法**:
```bash
# 1. 检查环境变量
ssh root@47.92.96.143 "cat /root/word-app/.env"

# 2. 检查容器状态
ssh root@47.92.96.143 "cd /root/word-app && docker compose ps"

# 3. 重新部署
bash deploy-production.sh
```

### 问题3: 数据库连接失败

**检查项**:
1. 数据库容器是否运行
2. 数据库连接字符串是否正确
3. 网络是否正常

**解决方法**:
```bash
# 1. 检查数据库容器
ssh root@47.92.96.143 "docker ps | grep postgres"

# 2. 查看数据库日志
ssh root@47.92.96.143 "cd /root/word-app && docker compose logs postgres"

# 3. 重启数据库
ssh root@47.92.96.143 "cd /root/word-app && docker compose restart postgres"
```

---

## 📞 联系支持

如遇到无法解决的问题，请联系技术支持：
- 查看部署日志
- 提供错误信息
- 说明操作步骤

---

**最后更新**: 2025-12-10
**维护者**: 开发团队
