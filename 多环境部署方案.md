# 多环境部署方案

## 🎯 环境规划

### 三个环境
1. **开发环境（Development）**
   - 平台：本地开发
   - 用途：日常开发和调试
   - 数据库：本地SQLite或PostgreSQL

2. **预览环境（Preview/Staging）**
   - 平台：Vercel
   - 用途：功能测试、演示
   - 数据库：Vercel Postgres
   - 自动部署：推送到GitHub自动部署

3. **生产环境（Production）**
   - 平台：阿里云服务器
   - 用途：正式上线使用
   - 数据库：PostgreSQL（Docker）
   - 部署方式：手动执行脚本或CI/CD

---

## 🌿 Git分支策略

### 分支结构
```
main (生产环境)
  ├── develop (开发环境)
  │   ├── feature/xxx (功能分支)
  │   └── bugfix/xxx (修复分支)
  └── hotfix/xxx (紧急修复)
```

### 分支说明

#### 1. main分支（生产分支）
- **用途**：生产环境代码
- **部署**：阿里云服务器
- **保护**：只能通过PR合并
- **触发**：手动执行部署脚本

#### 2. develop分支（开发分支）
- **用途**：开发环境代码
- **部署**：Vercel自动部署
- **合并**：功能开发完成后合并到main

#### 3. feature分支（功能分支）
- **命名**：feature/功能名称
- **用途**：新功能开发
- **合并**：开发完成后合并到develop

#### 4. hotfix分支（紧急修复）
- **命名**：hotfix/问题描述
- **用途**：生产环境紧急修复
- **合并**：同时合并到main和develop

---

## 📁 环境变量管理

### 文件结构
```
项目根目录/
├── .env.local          # 本地开发环境（不提交）
├── .env.development    # 开发环境配置（可提交）
├── .env.preview        # Vercel预览环境（可提交）
├── .env.production     # 生产环境配置（不提交，服务器上配置）
└── .env.example        # 环境变量示例（提交）
```

### .env.example（示例文件）
```bash
# 数据库配置
DATABASE_URL="postgresql://user:password@host:5432/database"

# Vercel Blob存储
BLOB_READ_WRITE_TOKEN="your_token_here"

# 应用配置
NODE_ENV="development"
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
```

### .env.local（本地开发）
```bash
DATABASE_URL="file:./dev.db"
NODE_ENV="development"
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
```

### .env.preview（Vercel预览）
```bash
# Vercel会自动注入DATABASE_URL
NODE_ENV="preview"
NEXT_PUBLIC_API_URL="https://your-app.vercel.app/api"
```

### .env.production（阿里云生产）
```bash
DATABASE_URL="postgresql://word_user:word_pass_2024@postgres:5432/word_app"
BLOB_READ_WRITE_TOKEN="your_production_token"
NODE_ENV="production"
NEXT_PUBLIC_API_URL="http://47.92.96.143:3000/api"
```

---

## 🚀 部署流程

### 1. 开发流程

#### 新功能开发
```bash
# 1. 从develop创建功能分支
git checkout develop
git pull origin develop
git checkout -b feature/new-feature

# 2. 开发功能
# ... 编写代码 ...

# 3. 提交代码
git add .
git commit -m "feat: 添加新功能"

# 4. 推送到远程
git push origin feature/new-feature

# 5. 创建PR合并到develop
# 在GitHub上创建Pull Request: feature/new-feature -> develop
```

#### 发布到生产
```bash
# 1. 从develop合并到main
git checkout main
git pull origin main
git merge develop

# 2. 打标签
git tag -a v1.0.0 -m "Release v1.0.0"

# 3. 推送到远程
git push origin main
git push origin v1.0.0

# 4. 部署到阿里云
bash deploy-production.sh
```

### 2. Vercel自动部署

#### 配置Vercel
1. 连接GitHub仓库
2. 设置分支部署规则：
   - **Production Branch**: `main`
   - **Preview Branches**: `develop`, `feature/*`
3. 配置环境变量（在Vercel Dashboard）

#### 自动触发
- 推送到`develop`分支 → 自动部署到Vercel预览环境
- 推送到`main`分支 → 自动部署到Vercel生产环境（可选）

### 3. 阿里云手动部署

#### 部署脚本：deploy-production.sh
```bash
#!/bin/bash

echo "=== 开始部署到阿里云生产环境 ==="

# 1. 确认当前分支
BRANCH=$(git branch --show-current)
if [ "$BRANCH" != "main" ]; then
  echo "错误：只能从main分支部署到生产环境"
  echo "当前分支：$BRANCH"
  exit 1
fi

# 2. 确认代码已提交
if [ -n "$(git status --porcelain)" ]; then
  echo "错误：有未提交的更改"
  git status
  exit 1
fi

# 3. 确认部署
read -p "确认部署到生产环境？(yes/no): " confirm
if [ "$confirm" != "yes" ]; then
  echo "取消部署"
  exit 0
fi

# 4. 执行部署
SERVER="root@47.92.96.143"
REMOTE_DIR="/root/word-app"

echo "1. 上传项目文件..."
scp -r web-admin/src $SERVER:$REMOTE_DIR/web-admin/
scp -r web-admin/prisma $SERVER:$REMOTE_DIR/web-admin/
scp -r web-admin/public $SERVER:$REMOTE_DIR/web-admin/
scp web-admin/package.json $SERVER:$REMOTE_DIR/web-admin/
scp web-admin/package-lock.json $SERVER:$REMOTE_DIR/web-admin/
scp web-admin/tsconfig.json $SERVER:$REMOTE_DIR/web-admin/
scp web-admin/tailwind.config.ts $SERVER:$REMOTE_DIR/web-admin/
scp web-admin/Dockerfile $SERVER:$REMOTE_DIR/web-admin/
scp web-admin/.dockerignore $SERVER:$REMOTE_DIR/web-admin/

echo "2. 上传配置文件..."
scp docker-compose.yml $SERVER:$REMOTE_DIR/
scp .env.production $SERVER:$REMOTE_DIR/.env

echo "3. 重新构建并启动..."
ssh $SERVER "cd $REMOTE_DIR && docker compose up -d --build"

echo "4. 等待服务启动..."
sleep 30

echo "5. 检查服务状态..."
ssh $SERVER "cd $REMOTE_DIR && docker compose ps"

echo ""
echo "=== 部署完成 ==="
echo "应用地址: http://47.92.96.143:3000"
```

---

## 📱 小程序环境配置

### 方案1：环境变量切换（推荐）

#### app.js
```javascript
App({
  globalData: {
    userInfo: null,
    token: null,
    apiUrl: this.getApiUrl(),
  },

  // 根据环境获取API地址
  getApiUrl() {
    // 可以通过编译时环境变量或配置文件切换
    const env = 'production' // 'development' | 'preview' | 'production'

    const apiUrls = {
      development: 'http://localhost:3000/api',
      preview: 'https://your-app.vercel.app/api',
      production: 'http://47.92.96.143:3000/api'
    }

    return apiUrls[env]
  },

  // ... 其他代码
})
```

### 方案2：配置文件切换

#### config/env.js
```javascript
// 环境配置
const ENV = 'production' // 手动切换环境

const config = {
  development: {
    apiUrl: 'http://localhost:3000/api',
    debug: true
  },
  preview: {
    apiUrl: 'https://your-app.vercel.app/api',
    debug: true
  },
  production: {
    apiUrl: 'http://47.92.96.143:3000/api',
    debug: false
  }
}

module.exports = config[ENV]
```

#### app.js
```javascript
const envConfig = require('./config/env.js')

App({
  globalData: {
    userInfo: null,
    token: null,
    apiUrl: envConfig.apiUrl,
  },
  // ... 其他代码
})
```

---

## 🔄 CI/CD自动化（可选）

### GitHub Actions配置

#### .github/workflows/deploy-production.yml
```yaml
name: Deploy to Aliyun Production

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.7.0
      with:
        ssh-private-key: ${{ secrets.ALIYUN_SSH_KEY }}

    - name: Deploy to Aliyun
      run: |
        bash deploy-production.sh
      env:
        SERVER_HOST: ${{ secrets.SERVER_HOST }}
        SERVER_USER: ${{ secrets.SERVER_USER }}
```

### 配置Secrets
在GitHub仓库设置中添加：
- `ALIYUN_SSH_KEY`: SSH私钥
- `SERVER_HOST`: 服务器地址
- `SERVER_USER`: 服务器用户名

---

## 📊 环境对比表

| 特性 | 本地开发 | Vercel预览 | 阿里云生产 |
|------|---------|-----------|-----------|
| **分支** | feature/* | develop | main |
| **数据库** | SQLite/本地PG | Vercel Postgres | Docker PostgreSQL |
| **部署方式** | 本地运行 | 自动部署 | 手动/CI/CD |
| **访问地址** | localhost:3000 | vercel.app | 47.92.96.143:3000 |
| **用途** | 开发调试 | 功能测试 | 正式使用 |
| **数据** | 测试数据 | 测试数据 | 真实数据 |

---

## 📝 .gitignore配置

```gitignore
# 环境变量
.env.local
.env.production
.env*.local

# 依赖
node_modules/
.pnp
.pnp.js

# 构建产物
.next/
out/
build/
dist/

# 数据库
*.db
*.db-journal
prisma/dev.db

# 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 系统文件
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# 临时文件
*.log
*.tmp
temp/
```

---

## 🎯 最佳实践

### 1. 代码管理
- ✅ 使用Git Flow工作流
- ✅ 功能开发在feature分支
- ✅ 代码审查后合并
- ✅ 使用语义化版本标签

### 2. 环境隔离
- ✅ 不同环境使用不同数据库
- ✅ 环境变量严格区分
- ✅ 生产环境配置不提交到Git

### 3. 部署安全
- ✅ 生产部署需要确认
- ✅ 部署前检查分支和提交状态
- ✅ 部署后验证服务状态
- ✅ 保留部署日志

### 4. 数据安全
- ✅ 定期备份生产数据库
- ✅ 测试环境使用脱敏数据
- ✅ 敏感信息使用环境变量

---

## 🚦 快速开始

### 初始化项目
```bash
# 1. 克隆仓库
git clone https://github.com/your-username/word-app.git
cd word-app

# 2. 安装依赖
cd web-admin
npm install

# 3. 配置环境变量
cp .env.example .env.local
# 编辑.env.local配置本地数据库

# 4. 初始化数据库
npx prisma generate
npx prisma db push

# 5. 启动开发服务器
npm run dev
```

### 部署到Vercel
```bash
# 1. 推送到develop分支
git checkout develop
git add .
git commit -m "feat: 新功能"
git push origin develop

# 2. Vercel自动部署
# 访问Vercel Dashboard查看部署状态
```

### 部署到阿里云
```bash
# 1. 合并到main分支
git checkout main
git merge develop
git push origin main

# 2. 执行部署脚本
bash deploy-production.sh

# 3. 验证部署
curl http://47.92.96.143:3000/api/health
```

---

## 📚 相关文档

- [Git Flow工作流](https://nvie.com/posts/a-successful-git-branching-model/)
- [Vercel部署文档](https://vercel.com/docs)
- [Docker Compose文档](https://docs.docker.com/compose/)
- [GitHub Actions文档](https://docs.github.com/actions)

---

**最后更新**: 2025-12-10
**维护者**: 开发团队
