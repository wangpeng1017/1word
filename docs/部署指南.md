# 部署指南

## 环境概览

| 环境 | 服务器 | 数据库 | 域名 | 用途 |
|------|--------|--------|------|------|
| 测试 | 阿里云 8.130.182.148 | MySQL (本地) | - | 测试验证 |
| 生产 | 新东方 47.94.235.91 | 阿里云 RDS MySQL | ienglish.xdf.cn | 正式使用 |

---

## 测试环境（阿里云 8.130.182.148）

### 服务器信息

| 项目 | 值 |
|------|-----|
| **IP** | 8.130.182.148 |
| **系统** | CentOS 7 |
| **应用目录** | /root/word-app/web-admin |
| **应用端口** | 3000 |
| **访问地址** | http://8.130.182.148:3000 |

### 数据库配置

```bash
# MySQL 连接信息
DATABASE_URL="mysql://word_mysql:word_mysql_2024@localhost:3306/word_app_mysql"
```

### 快速更新部署

```bash
# 1. 拉取最新代码
ssh root@8.130.182.148 "cd /root/word-app && git pull origin main"

# 2. 安装依赖（如有新增）
ssh root@8.130.182.148 "cd /root/word-app/web-admin && npm install --legacy-peer-deps"

# 3. 同步数据库（如有变更）
ssh root@8.130.182.148 "cd /root/word-app/web-admin && npx prisma db push"

# 4. 重新构建
ssh root@8.130.182.148 "cd /root/word-app/web-admin && npm run build"

# 5. 重启应用
ssh root@8.130.182.148 "pkill -f 'next start'; cd /root/word-app/web-admin && nohup npm start > /tmp/app.log 2>&1 &"
```

### 一键更新脚本

```bash
ssh root@8.130.182.148 "cd /root/word-app && git pull origin main && cd web-admin && npm install --legacy-peer-deps && npx prisma db push && npm run build && pkill -f 'next start'; nohup npm start > /tmp/app.log 2>&1 &"
```

---

## 生产环境（新东方服务器）

详见 [XDF_部署方案_完整版.md](./XDF_部署方案_完整版.md)

### 关键信息

| 项目 | 值 |
|------|-----|
| **服务器 IP** | 内网: 172.20.234.44 / 公网: 47.94.235.91 |
| **域名** | https://ienglish.xdf.cn |
| **数据库** | 阿里云 RDS MySQL |
| **进程管理** | PM2 (word-app) |
| **应用目录** | /home/dontovertime/apps/1word/web-admin |

### 数据库连接

```bash
DATABASE_URL="mysql://PRO_RDS_bdcxcx_RW:4n8anApuMflp3cRr@rm-2zel9bu41o5s0v0j8.mysql.rds.aliyuncs.com:3306/bdcxcx"
```

---

## 数据导入脚本

### 导入练习题

```bash
# 1. 解析 Word 文档（本地执行）
npx ts-node scripts/parse-questions-v2.ts

# 2. 导入到数据库（服务器执行）
npx ts-node scripts/import-questions-v2.ts
```

### 导出数据（用于迁移）

```bash
# 导出题目数据为 JSON
npx ts-node scripts/export-questions.ts
```

---

## 常用运维命令

### 测试服务器

```bash
# 查看应用状态
ssh root@8.130.182.148 "ps aux | grep next"

# 查看日志
ssh root@8.130.182.148 "tail -100 /tmp/app.log"

# 重启应用
ssh root@8.130.182.148 "pkill -f 'next start'; cd /root/word-app/web-admin && nohup npm start > /tmp/app.log 2>&1 &"
```

### 生产服务器

```bash
# 通过堡垒机登录后执行

# 查看应用状态
pm2 status

# 查看日志
pm2 logs word-app --lines 50

# 重启应用
pm2 restart word-app
```

---

## 小程序环境配置

编辑 `wechat-miniapp/config/env.js`：

```javascript
// 测试环境
const ENV = 'production'  // 使用阿里云 8.130 服务器

// 新东方生产环境
const ENV = 'xdf'  // 使用 ienglish.xdf.cn
```
