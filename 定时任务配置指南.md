# 定时任务配置指南

## 📋 概述

系统需要定时重置周期性积分，以保证日榜、周榜、月榜的正常运作。

---

## 🔄 需要重置的积分

### 1. 每日积分（dailyPoints）
- **重置时间**：每天 00:00
- **用途**：日排行榜

### 2. 每周积分（weeklyPoints）
- **重置时间**：每周一 00:00
- **用途**：周排行榜

### 3. 每月积分（monthlyPoints）
- **重置时间**：每月 1 日 00:00
- **用途**：月排行榜

**注意**：总积分（totalPoints）永久累计，不需要重置。

---

## 🛠️ 实现方式

### 方式一：使用脚本（推荐用于开发/测试）

#### 1. 手动运行脚本

```bash
# 进入项目目录
cd web-admin

# 重置每日积分
node scripts/reset-points.js daily

# 重置每周积分
node scripts/reset-points.js weekly

# 重置每月积分
node scripts/reset-points.js monthly

# 重置所有周期性积分
node scripts/reset-points.js all
```

#### 2. 使用 cron（Linux/Mac）

编辑 crontab：
```bash
crontab -e
```

添加以下任务：
```cron
# 每天 00:00 重置每日积分
0 0 * * * cd /path/to/web-admin && node scripts/reset-points.js daily

# 每周一 00:00 重置每周积分
0 0 * * 1 cd /path/to/web-admin && node scripts/reset-points.js weekly

# 每月 1 日 00:00 重置每月积分
0 0 1 * * cd /path/to/web-admin && node scripts/reset-points.js monthly
```

#### 3. 使用 Windows 任务计划程序

1. 打开"任务计划程序"
2. 创建基本任务
3. 设置触发器（每天/每周/每月）
4. 操作：启动程序
   - 程序：`node`
   - 参数：`scripts/reset-points.js daily`
   - 起始于：`E:\trae\1单词\web-admin`

---

### 方式二：使用 API 端点（推荐用于生产环境）

#### 1. API 端点

```
POST /api/cron/reset-points
```

#### 2. 请求格式

```bash
curl -X POST https://your-domain.com/api/cron/reset-points \
  -H "Authorization: Bearer your-secret-key" \
  -H "Content-Type: application/json" \
  -d '{"type": "daily"}'
```

**参数**：
- `type`: 重置类型（daily, weekly, monthly, all）

**认证**：
- 需要在请求头中提供 `Authorization: Bearer {CRON_SECRET}`
- `CRON_SECRET` 在环境变量中配置

#### 3. 环境变量配置

在 `.env` 文件中添加：
```env
CRON_SECRET=your-secret-key-here
```

**安全建议**：
- 使用强密码作为 CRON_SECRET
- 不要将密钥提交到 Git
- 定期更换密钥

---

### 方式三：使用 Vercel Cron（推荐用于 Vercel 部署）

#### 1. 配置文件

已创建 `vercel-cron.json`：
```json
{
  "crons": [
    {
      "path": "/api/cron/reset-points",
      "schedule": "0 0 * * *"
    }
  ]
}
```

#### 2. 部署配置

在 `vercel.json` 中添加：
```json
{
  "crons": [
    {
      "path": "/api/cron/reset-points",
      "schedule": "0 0 * * *"
    }
  ]
}
```

#### 3. Cron 表达式

```
0 0 * * *     # 每天 00:00
0 0 * * 1     # 每周一 00:00
0 0 1 * *     # 每月 1 日 00:00
```

#### 4. 环境变量

在 Vercel 项目设置中添加：
- `CRON_SECRET`: 你的密钥

#### 5. 多个定时任务

如果需要分别配置日/周/月重置：

```json
{
  "crons": [
    {
      "path": "/api/cron/reset-points?type=daily",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/cron/reset-points?type=weekly",
      "schedule": "0 0 * * 1"
    },
    {
      "path": "/api/cron/reset-points?type=monthly",
      "schedule": "0 0 1 * *"
    }
  ]
}
```

---

### 方式四：使用第三方定时任务服务

#### 1. EasyCron (https://www.easycron.com)

- 免费版支持基本定时任务
- 配置简单，无需服务器

**配置步骤**：
1. 注册账号
2. 创建新任务
3. URL: `https://your-domain.com/api/cron/reset-points`
4. Method: POST
5. Headers: `Authorization: Bearer your-secret-key`
6. Body: `{"type": "daily"}`
7. Schedule: 每天 00:00

#### 2. cron-job.org (https://cron-job.org)

- 免费服务
- 支持 HTTP 请求

**配置步骤**：
1. 注册账号
2. 创建新任务
3. URL: `https://your-domain.com/api/cron/reset-points`
4. Request Method: POST
5. Request Body: `{"type": "daily"}`
6. Headers: `Authorization: Bearer your-secret-key`
7. Schedule: 每天 00:00

---

## 🧪 测试定时任务

### 1. 测试脚本

```bash
# 测试每日积分重置
node scripts/reset-points.js daily

# 查看输出，确认重置成功
```

### 2. 测试 API

```bash
# 测试 API 端点
curl -X POST http://localhost:3000/api/cron/reset-points \
  -H "Authorization: Bearer your-secret-key" \
  -H "Content-Type: application/json" \
  -d '{"type": "daily"}'

# 预期输出
{
  "success": true,
  "data": {
    "message": "积分重置成功",
    "type": "daily",
    "result": {
      "daily": {
        "count": 10
      }
    },
    "timestamp": "2025-01-01T00:00:00.000Z"
  }
}
```

### 3. 验证数据库

```sql
-- 查看所有学生的积分
SELECT
  id,
  studentId,
  totalPoints,
  dailyPoints,
  weeklyPoints,
  monthlyPoints,
  level
FROM student_points;

-- 验证每日积分是否为 0
SELECT COUNT(*) FROM student_points WHERE dailyPoints > 0;
```

---

## 📊 监控和日志

### 1. 查看执行日志

脚本会输出执行日志：
```
📅 积分重置任务 - 2025/1/1 00:00:00
重置类型: daily

开始重置每日积分...
✅ 已重置 10 个学生的每日积分

✅ 积分重置完成！
```

### 2. 监控建议

- 设置定时任务执行失败告警
- 记录每次重置的学生数量
- 定期检查积分数据是否正常

---

## ⚠️ 注意事项

### 1. 时区问题

- 确保服务器时区设置正确
- Vercel Cron 使用 UTC 时间
- 如果需要北京时间（UTC+8），需要调整 cron 表达式

**示例**：
```
# 北京时间 00:00 = UTC 16:00（前一天）
0 16 * * *    # 每天 UTC 16:00（北京时间 00:00）
```

### 2. 数据备份

- 重置前建议备份数据
- 可以在脚本中添加备份逻辑

### 3. 并发问题

- 确保同一时间只有一个重置任务在运行
- 避免重复重置

### 4. 错误处理

- 脚本包含错误处理逻辑
- 失败时会输出错误信息
- 建议配置告警通知

---

## 🚀 快速开始

### 开发环境

```bash
# 1. 测试脚本
cd web-admin
node scripts/reset-points.js daily

# 2. 配置本地 cron（可选）
# Linux/Mac: 编辑 crontab
# Windows: 使用任务计划程序
```

### 生产环境（Vercel）

```bash
# 1. 配置环境变量
# 在 Vercel 项目设置中添加 CRON_SECRET

# 2. 更新 vercel.json
# 添加 crons 配置

# 3. 部署
vercel --prod

# 4. 验证
# 在 Vercel Dashboard 查看 Cron 任务状态
```

---

## 📝 总结

推荐方案：
- **开发环境**：手动运行脚本
- **生产环境（Vercel）**：使用 Vercel Cron
- **生产环境（其他）**：使用系统 cron 或第三方服务

所有方式都已实现，可根据实际部署环境选择合适的方案。
