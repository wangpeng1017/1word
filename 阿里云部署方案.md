# 阿里云部署方案（方案 A：完整部署）

## 📋 概述

本文档提供了将"1单词"项目从 Vercel 迁移到阿里云服务器的完整部署方案。采用 **Cloudflare Tunnel** 作为临时 HTTPS 解决方案，解决域名未备案问题。

## 🖥️ 服务器信息

- **服务器 IP**: 47.92.96.143
- **域名**: 111word.aifly.me（⚠️ 未备案）
- **数据库**: 自建 PostgreSQL
- **文件存储**: 需要开通阿里云 OSS
- **HTTPS**: 使用 Cloudflare Tunnel（临时方案）

## ⚠️ 重要提醒

### 域名备案问题
- 国内服务器必须使用已备案的域名
- 域名备案通常需要 15-20 个工作日
- 在备案期间，使用 Cloudflare Tunnel 提供 HTTPS 访问

### 小程序 HTTPS 要求
- 微信小程序强制要求使用 HTTPS
- 必须配置有效的 SSL 证书
- 服务器域名必须在微信公众平台配置

## 🎯 部署步骤

### 阶段 0：环境检查和准备工作

#### 0.1 连接服务器
```bash
# 使用 SSH 连接服务器
ssh root@47.92.96.143

# 或使用密钥
ssh -i /path/to/key.pem root@47.92.96.143
```

#### 0.2 检查系统环境
```bash
# 检查操作系统版本
cat /etc/os-release

# 检查磁盘空间（至少需要 20GB 可用空间）
df -h

# 检查内存（建议至少 2GB）
free -h

# 检查 CPU
lscpu
```

#### 0.3 安装基础软件
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y  # Ubuntu/Debian
# 或
sudo yum update -y  # CentOS/RHEL

# 安装 Node.js 20+
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# 验证安装
node --version  # 应该显示 v20.x.x
npm --version

# 安装 Git
sudo apt install -y git

# 安装 PM2（进程管理器）
sudo npm install -g pm2

# 安装 Nginx
sudo apt install -y nginx

# 启动 Nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

#### 0.4 安装和配置 PostgreSQL
```bash
# 安装 PostgreSQL
sudo apt install -y postgresql postgresql-contrib

# 启动 PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 创建数据库和用户
sudo -u postgres psql

# 在 PostgreSQL 命令行中执行：
CREATE DATABASE word_app;
CREATE USER word_user WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE word_app TO word_user;
\q
```

#### 0.5 配置防火墙
```bash
# 允许 SSH、HTTP、HTTPS
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 3000/tcp  # Next.js 应用端口
sudo ufw enable
```

#### 0.6 开通阿里云 OSS

1. 登录阿里云控制台：https://oss.console.aliyun.com/
2. 点击"创建 Bucket"
3. 配置 Bucket：
   - **Bucket 名称**: word-app-storage（或其他名称）
   - **地域**: 选择与服务器相同的地域
   - **存储类型**: 标准存储
   - **读写权限**: 私有
   - **服务端加密**: 无
4. 创建 AccessKey：
   - 进入"访问控制" > "用户" > "创建用户"
   - 勾选"编程访问"
   - 保存 AccessKey ID 和 AccessKey Secret
5. 配置跨域规则（CORS）：
   - 进入 Bucket > 权限管理 > 跨域设置
   - 添加规则：
     - 来源：`*`（或指定域名）
     - 允许 Methods：GET, POST, PUT, DELETE, HEAD
     - 允许 Headers：`*`
     - 暴露 Headers：ETag

---

### 阶段 1：配置 Cloudflare Tunnel（临时 HTTPS 方案）

#### 1.1 安装 Cloudflare Tunnel
```bash
# 下载并安装 cloudflared
wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
sudo dpkg -i cloudflared-linux-amd64.deb

# 验证安装
cloudflared --version
```

#### 1.2 登录 Cloudflare
```bash
# 登录（会打开浏览器）
cloudflared tunnel login
```

#### 1.3 创建 Tunnel
```bash
# 创建 tunnel
cloudflared tunnel create word-app

# 记录 Tunnel ID（会显示在输出中）
# 例如：Created tunnel word-app with id xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

#### 1.4 配置 DNS
```bash
# 将域名指向 tunnel
cloudflared tunnel route dns word-app 111word.aifly.me
```

#### 1.5 创建配置文件
```bash
# 创建配置文件
sudo mkdir -p /etc/cloudflared
sudo nano /etc/cloudflared/config.yml
```

配置文件内容：
```yaml
tunnel: <your-tunnel-id>
credentials-file: /root/.cloudflared/<your-tunnel-id>.json

ingress:
  - hostname: 111word.aifly.me
    service: http://localhost:3000
  - service: http_status:404
```

#### 1.6 启动 Tunnel 服务
```bash
# 安装为系统服务
sudo cloudflared service install

# 启动服务
sudo systemctl start cloudflared
sudo systemctl enable cloudflared

# 检查状态
sudo systemctl status cloudflared
```

---

### 阶段 2：数据库迁移

#### 2.1 从 Vercel Postgres 导出数据

在本地执行：
```bash
# 进入项目目录
cd E:\trae\1单词\web-admin

# 使用 Prisma 导出数据
npx prisma db pull

# 生成 SQL 导出脚本
pg_dump $DATABASE_URL > backup.sql
```

或者使用 Prisma Studio 手动导出：
```bash
npx prisma studio
# 在浏览器中打开，手动导出数据
```

#### 2.2 上传数据到服务器
```bash
# 从本地上传到服务器
scp backup.sql root@47.92.96.143:/tmp/
```

#### 2.3 导入数据到阿里云 PostgreSQL
```bash
# 在服务器上执行
ssh root@47.92.96.143

# 导入数据
psql -U word_user -d word_app -f /tmp/backup.sql

# 验证数据
psql -U word_user -d word_app
\dt  # 查看所有表
SELECT COUNT(*) FROM "Vocabulary";  # 检查词汇数量
\q
```

---

### 阶段 3：部署 Next.js 应用

#### 3.1 克隆代码
```bash
# 在服务器上执行
cd /var/www
sudo git clone <your-git-repo-url> word-app
cd word-app/web-admin
```

#### 3.2 配置环境变量
```bash
# 创建生产环境配置
sudo nano .env.production
```

`.env.production` 内容：
```env
# 数据库配置
DATABASE_URL="postgresql://word_user:your_secure_password@localhost:5432/word_app"

# OSS 配置
OSS_REGION="oss-cn-hangzhou"  # 根据实际地域修改
OSS_BUCKET="word-app-storage"
OSS_ACCESS_KEY_ID="your_access_key_id"
OSS_ACCESS_KEY_SECRET="your_access_key_secret"

# 应用配置
NEXT_PUBLIC_API_URL="https://111word.aifly.me"
NODE_ENV="production"

# 微信小程序配置（如果需要）
WECHAT_APP_ID="your_app_id"
WECHAT_APP_SECRET="your_app_secret"
```

#### 3.3 安装依赖和构建
```bash
# 安装依赖
npm install

# 生成 Prisma Client
npx prisma generate

# 运行数据库迁移
npx prisma db push

# 构建应用
npm run build
```

#### 3.4 使用 PM2 启动应用
```bash
# 启动应用
pm2 start npm --name "word-app" -- start

# 设置开机自启
pm2 startup
pm2 save

# 查看日志
pm2 logs word-app

# 查看状态
pm2 status
```

#### 3.5 验证应用
```bash
# 测试本地访问
curl http://localhost:3000

# 应该返回 HTML 内容
```

---

### 阶段 4：文件存储迁移（Vercel Blob → OSS）

#### 4.1 修改上传代码

编辑 `web-admin/app/api/upload/route.ts`：

```typescript
import { put } from '@vercel/blob';
import { NextResponse } from 'next/server';
import OSS from 'ali-oss';

// 创建 OSS 客户端
const client = new OSS({
  region: process.env.OSS_REGION!,
  accessKeyId: process.env.OSS_ACCESS_KEY_ID!,
  accessKeySecret: process.env.OSS_ACCESS_KEY_SECRET!,
  bucket: process.env.OSS_BUCKET!,
});

export async function POST(request: Request) {
  const formData = await request.formData();
  const file = formData.get('file') as File;

  if (!file) {
    return NextResponse.json({ error: 'No file provided' }, { status: 400 });
  }

  try {
    // 生成唯一文件名
    const filename = `${Date.now()}-${file.name}`;

    // 上传到 OSS
    const buffer = Buffer.from(await file.arrayBuffer());
    const result = await client.put(filename, buffer);

    return NextResponse.json({
      url: result.url,
      name: result.name
    });
  } catch (error) {
    console.error('Upload error:', error);
    return NextResponse.json({ error: 'Upload failed' }, { status: 500 });
  }
}
```

#### 4.2 安装 OSS SDK
```bash
cd /var/www/word-app/web-admin
npm install ali-oss
```

#### 4.3 迁移现有文件

创建迁移脚本 `migrate-files.js`：
```javascript
const OSS = require('ali-oss');
const { PrismaClient } = require('@prisma/client');
const https = require('https');

const prisma = new PrismaClient();
const client = new OSS({
  region: process.env.OSS_REGION,
  accessKeyId: process.env.OSS_ACCESS_KEY_ID,
  accessKeySecret: process.env.OSS_ACCESS_KEY_SECRET,
  bucket: process.env.OSS_BUCKET,
});

async function migrateFiles() {
  // 获取所有包含 Vercel Blob URL 的记录
  const vocabularies = await prisma.vocabulary.findMany({
    where: {
      OR: [
        { audioUrl: { contains: 'vercel-storage' } },
        { imageUrl: { contains: 'vercel-storage' } },
      ]
    }
  });

  console.log(`Found ${vocabularies.length} records to migrate`);

  for (const vocab of vocabularies) {
    try {
      // 迁移音频文件
      if (vocab.audioUrl && vocab.audioUrl.includes('vercel-storage')) {
        const audioBuffer = await downloadFile(vocab.audioUrl);
        const audioFilename = `audio/${vocab.id}-${Date.now()}.mp3`;
        const audioResult = await client.put(audioFilename, audioBuffer);

        await prisma.vocabulary.update({
          where: { id: vocab.id },
          data: { audioUrl: audioResult.url }
        });

        console.log(`Migrated audio for ${vocab.word}`);
      }

      // 迁移图片文件
      if (vocab.imageUrl && vocab.imageUrl.includes('vercel-storage')) {
        const imageBuffer = await downloadFile(vocab.imageUrl);
        const imageFilename = `images/${vocab.id}-${Date.now()}.jpg`;
        const imageResult = await client.put(imageFilename, imageBuffer);

        await prisma.vocabulary.update({
          where: { id: vocab.id },
          data: { imageUrl: imageResult.url }
        });

        console.log(`Migrated image for ${vocab.word}`);
      }
    } catch (error) {
      console.error(`Error migrating ${vocab.word}:`, error);
    }
  }

  console.log('Migration complete!');
}

function downloadFile(url) {
  return new Promise((resolve, reject) => {
    https.get(url, (response) => {
      const chunks = [];
      response.on('data', (chunk) => chunks.push(chunk));
      response.on('end', () => resolve(Buffer.concat(chunks)));
      response.on('error', reject);
    });
  });
}

migrateFiles()
  .catch(console.error)
  .finally(() => prisma.$disconnect());
```

运行迁移：
```bash
node migrate-files.js
```

---

### 阶段 5：配置 Nginx（可选，如果不使用 Cloudflare Tunnel）

#### 5.1 创建 Nginx 配置
```bash
sudo nano /etc/nginx/sites-available/word-app
```

配置内容：
```nginx
server {
    listen 80;
    server_name 111word.aifly.me;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

#### 5.2 启用配置
```bash
sudo ln -s /etc/nginx/sites-available/word-app /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### 5.3 配置 HTTPS（Let's Encrypt）
```bash
# 安装 Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取证书（域名备案后才能使用）
sudo certbot --nginx -d 111word.aifly.me

# 自动续期
sudo certbot renew --dry-run
```

---

### 阶段 6：更新小程序配置

#### 6.1 修改小程序 API 地址

编辑 `wechat-miniapp/app.js`（第 6 行）：
```javascript
App({
  globalData: {
    // 修改为新的 API 地址
    apiUrl: 'https://111word.aifly.me',
    userInfo: null
  },

  // ... 其他代码
})
```

#### 6.2 配置微信公众平台

1. 登录微信公众平台：https://mp.weixin.qq.com/
2. 进入"开发" > "开发管理" > "开发设置"
3. 配置服务器域名：
   - **request 合法域名**: `https://111word.aifly.me`
   - **uploadFile 合法域名**: `https://111word.aifly.me`
   - **downloadFile 合法域名**: `https://111word.aifly.me`（或 OSS 域名）

#### 6.3 重新上传小程序
```bash
# 在本地开发环境
cd wechat-miniapp

# 使用微信开发者工具上传
# 或使用命令行工具
```

---

### 阶段 7：配置定时任务

#### 7.1 创建定时任务脚本

创建 `cron-reset-points.sh`：
```bash
#!/bin/bash
cd /var/www/word-app/web-admin
curl -X POST http://localhost:3000/api/cron/reset-points
```

赋予执行权限：
```bash
chmod +x cron-reset-points.sh
```

#### 7.2 配置 Cron
```bash
# 编辑 crontab
crontab -e

# 添加定时任务（每天凌晨 0 点执行）
0 0 * * * /var/www/word-app/web-admin/cron-reset-points.sh >> /var/log/cron-reset-points.log 2>&1
```

#### 7.3 验证定时任务
```bash
# 查看 cron 日志
tail -f /var/log/cron-reset-points.log

# 手动测试
./cron-reset-points.sh
```

---

### 阶段 8：测试验证

#### 8.1 测试管理后台
```bash
# 测试 API 健康检查
curl https://111word.aifly.me/api/health

# 测试登录
curl -X POST https://111word.aifly.me/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"your_password"}'
```

#### 8.2 测试小程序功能
1. 打开小程序
2. 测试登录功能
3. 测试词汇列表加载
4. 测试音频播放
5. 测试图片显示
6. 测试答题功能

#### 8.3 测试文件上传
1. 登录管理后台
2. 上传音频文件
3. 上传图片文件
4. 验证文件 URL 是否为 OSS 地址

#### 8.4 测试定时任务
```bash
# 查看定时任务日志
tail -f /var/log/cron-reset-points.log

# 检查数据库中的积分是否重置
psql -U word_user -d word_app
SELECT username, points FROM "User" LIMIT 10;
\q
```

---

## 📍 关键配置位置

### 代码文件
- **小程序 API 配置**: `wechat-miniapp/app.js` (第 6 行)
- **文件上传 API**: `web-admin/app/api/upload/route.ts`
- **定时任务 API**: `web-admin/app/api/cron/reset-points/route.ts`
- **环境变量**: `web-admin/.env.production`

### 服务器配置
- **应用目录**: `/var/www/word-app`
- **Nginx 配置**: `/etc/nginx/sites-available/word-app`
- **Cloudflare 配置**: `/etc/cloudflared/config.yml`
- **PM2 配置**: `~/.pm2/`
- **Cron 日志**: `/var/log/cron-reset-points.log`

---

## 🔧 常见问题和故障排除

### 1. 应用无法启动
```bash
# 查看 PM2 日志
pm2 logs word-app

# 查看详细错误
pm2 describe word-app

# 重启应用
pm2 restart word-app
```

### 2. 数据库连接失败
```bash
# 检查 PostgreSQL 状态
sudo systemctl status postgresql

# 测试数据库连接
psql -U word_user -d word_app

# 检查环境变量
cat /var/www/word-app/web-admin/.env.production
```

### 3. Cloudflare Tunnel 无法连接
```bash
# 检查 cloudflared 状态
sudo systemctl status cloudflared

# 查看日志
sudo journalctl -u cloudflared -f

# 重启服务
sudo systemctl restart cloudflared
```

### 4. 文件上传失败
```bash
# 检查 OSS 配置
echo $OSS_ACCESS_KEY_ID
echo $OSS_BUCKET

# 测试 OSS 连接
node -e "const OSS = require('ali-oss'); const client = new OSS({region: process.env.OSS_REGION, accessKeyId: process.env.OSS_ACCESS_KEY_ID, accessKeySecret: process.env.OSS_ACCESS_KEY_SECRET, bucket: process.env.OSS_BUCKET}); client.list().then(console.log).catch(console.error);"
```

### 5. 小程序无法连接服务器
- 检查微信公众平台的服务器域名配置
- 确认 HTTPS 证书有效
- 检查防火墙规则
- 使用微信开发者工具的"详情" > "域名信息"查看连接状态

### 6. 定时任务未执行
```bash
# 检查 cron 服务
sudo systemctl status cron

# 查看 cron 日志
grep CRON /var/log/syslog

# 手动执行测试
/var/www/word-app/web-admin/cron-reset-points.sh
```

---

## 📝 备份和恢复

### 数据库备份
```bash
# 创建备份脚本
cat > /var/www/word-app/backup-db.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/var/backups/word-app"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
pg_dump -U word_user word_app > $BACKUP_DIR/backup_$DATE.sql
# 保留最近 7 天的备份
find $BACKUP_DIR -name "backup_*.sql" -mtime +7 -delete
EOF

chmod +x /var/www/word-app/backup-db.sh

# 添加到 crontab（每天凌晨 2 点备份）
crontab -e
# 添加：2 0 * * * /var/www/word-app/backup-db.sh
```

### 数据库恢复
```bash
# 恢复数据库
psql -U word_user -d word_app < /var/backups/word-app/backup_YYYYMMDD_HHMMSS.sql
```

---

## 🚀 下一步行动

### 立即执行
1. ✅ 连接服务器检查环境（阶段 0）
2. ✅ 安装 Cloudflare Tunnel（阶段 1）
3. ✅ 迁移数据库（阶段 2）
4. ✅ 部署应用（阶段 3）

### 并行执行
- 🔄 开通阿里云 OSS（阶段 0.6）
- 🔄 启动域名备案流程

### 备案完成后
- 📋 配置 Nginx + Let's Encrypt（阶段 5）
- 📋 切换到正式域名
- 📋 关闭 Cloudflare Tunnel

---

## 📞 需要帮助？

如果在部署过程中遇到问题，请提供：
1. 错误日志（PM2、Nginx、Cloudflared）
2. 服务器环境信息（OS、Node.js 版本）
3. 具体的错误信息和步骤

---

**文档版本**: 1.0
**创建日期**: 2025-12-09
**最后更新**: 2025-12-09
