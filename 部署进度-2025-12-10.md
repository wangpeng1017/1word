# 阿里云部署进度记录 - 2025-12-10

## 部署环境
- 服务器IP: 47.92.96.143
- 应用端口: 3000
- 数据库: PostgreSQL 15
- 容器管理: Docker Compose

## 已完成工作

### 1. Docker配置文件准备 ✅
- ✅ 创建 `docker-compose.yml` (PostgreSQL + Next.js应用)
- ✅ 创建 `web-admin/Dockerfile` (Node.js 20 Alpine + OpenSSL)
- ✅ 创建 `.env.server` (环境变量配置)
- ✅ 创建 `deploy.sh` (自动化部署脚本)

### 2. 数据库配置 ✅
- ✅ Schema配置为PostgreSQL
- ✅ 准备数据导出文件 `web-admin/database-export.json` (1403条记录)
- ✅ 准备数据导入脚本 `web-admin/import-database.js`

### 3. 部署脚本优化 ✅
- ✅ 修改rsync为scp命令（Windows兼容）
- ✅ 修改docker-compose为docker compose（新版本命令）
- ✅ 修改Dockerfile使用openssl替代openssl1.1-compat

### 4. SSH连接测试 ✅
- ✅ SSH连接正常
- ✅ Docker环境正常

### 5. 部署执行 ✅
- ✅ 执行deploy.sh自动化部署脚本
- ✅ 上传所有项目文件到服务器
- ✅ Docker镜像构建成功（约5分钟）
- ✅ 容器启动成功
- ✅ 数据库迁移完成
- ✅ 数据导入成功（1403条记录）

### 6. 部署验证 ✅
- ✅ 容器状态正常（word-app和postgres-word都在运行）
- ✅ 应用启动成功（Next.js 15.5.7）
- ✅ HTTP访问测试通过（返回200状态码）
- ✅ 数据库连接正常

## 遇到的问题及解决方案

### 问题1: SQLite兼容性问题 ❌
**问题描述**:
尝试使用SQLite替代PostgreSQL时，发现schema.prisma中有大量SQLite不支持的特性：
- `@db.Date` 类型
- `String[]` 数组类型
- `Json` 类型
- `enum` 枚举类型

**解决方案**:
恢复使用PostgreSQL配置，保持所有功能完整性。

### 问题2: Docker构建失败 ❌
**问题描述**:
- 第一次构建失败：`openssl1.1-compat` 包不存在
- 第二次构建失败：SQLite schema验证错误

**解决方案**:
- 修改Dockerfile使用 `openssl` 替代 `openssl1.1-compat`
- 恢复PostgreSQL配置

### 问题3: 部署脚本兼容性 ✅
**问题描述**:
- Windows环境没有rsync命令
- 服务器使用新版docker compose命令

**解决方案**:
- 使用scp命令替代rsync
- 使用`docker compose`替代`docker-compose`

## 当前状态

### 配置文件状态
```
✅ docker-compose.yml - PostgreSQL配置
✅ web-admin/Dockerfile - OpenSSL配置
✅ web-admin/prisma/schema.prisma - PostgreSQL配置
✅ deploy.sh - 部署脚本
✅ .env.server - 环境变量
✅ web-admin/database-export.json - 数据文件
✅ web-admin/import-database.js - 导入脚本
```

### 服务器状态
- ✅ 应用容器运行中（word-app）
- ✅ 数据库容器运行中（postgres-word）
- ✅ 应用可正常访问（http://47.92.96.143:3000）
- ✅ 数据已成功导入

## 部署成功摘要

### 部署结果
- ✅ **部署状态**: 成功
- ✅ **部署时间**: 2025-12-10 20:05
- ✅ **构建时长**: 约5分钟
- ✅ **应用地址**: http://47.92.96.143:3000
- ✅ **数据导入**: 1403条记录全部导入成功

### 数据导入详情
- ✅ user: 6 条记录
- ✅ teachers: 2 条记录
- ✅ classes: 5 条记录
- ✅ students: 4 条记录
- ✅ vocabularies: 50 条记录
- ✅ word_meanings: 74 条记录
- ✅ word_audios: 51 条记录
- ✅ questions: 200 条记录
- ✅ question_options: 800 条记录
- ✅ study_plans: 9 条记录
- ✅ plan_classes: 9 条记录
- ✅ daily_tasks: 9 条记录
- ✅ study_records: 6 条记录
- ✅ word_masteries: 89 条记录
- ✅ wrong_questions: 70 条记录
- ✅ achievements: 20 条记录

### 容器状态
```
word-app        运行中  (端口: 3000)
postgres-word   运行中  (端口: 5432, 健康状态: healthy)
```

## 后续待办事项

### 1. 功能测试（待完成）
- ⚠️ 登录功能测试
- ⚠️ 词汇管理测试
- ⚠️ 学习计划测试
- ⚠️ 测试记录测试
- ⚠️ 学生管理测试
- ⚠️ 班级管理测试

### 2. 配置定时任务（待完成）
需要配置定时任务来处理：
- ⚠️ 每日任务生成
- ⚠️ 数据统计
- ⚠️ 积分重置
- ⚠️ 其他定时操作

### 3. 性能优化（待完成）
- ⚠️ 数据库查询优化
- ⚠️ 缓存配置
- ⚠️ 静态资源优化

### 4. 监控和日志（待完成）
- ⚠️ 应用监控配置
- ⚠️ 日志收集配置
- ⚠️ 告警配置

## 快速恢复命令

如果需要快速重新部署，执行以下命令：

```bash
# 1. 停止并删除旧容器
ssh root@47.92.96.143 "cd /root/word-app && docker compose down"

# 2. 上传最新配置
scp docker-compose.yml root@47.92.96.143:/root/word-app/
scp web-admin/prisma/schema.prisma root@47.92.96.143:/root/word-app/web-admin/prisma/
scp web-admin/Dockerfile root@47.92.96.143:/root/word-app/web-admin/

# 3. 重新构建并启动
ssh root@47.92.96.143 "cd /root/word-app && docker compose up -d --build"

# 4. 查看日志
ssh root@47.92.96.143 "cd /root/word-app && docker compose logs -f"
```

## 重要文件清单

### 本地文件
- `docker-compose.yml` - Docker编排配置
- `deploy.sh` - 自动化部署脚本
- `.env.server` - 环境变量配置
- `web-admin/Dockerfile` - 应用镜像构建文件
- `web-admin/prisma/schema.prisma` - 数据库Schema
- `web-admin/database-export.json` - 数据导出文件（1403条记录）
- `web-admin/import-database.js` - 数据导入脚本

### 服务器文件路径
- 项目目录: `/root/word-app/`
- 应用代码: `/root/word-app/web-admin/`
- 数据文件: `/root/word-app/database-export.json`
- 导入脚本: `/root/word-app/import-database.js`

## 数据库连接信息

```
Host: postgres (容器内) / 47.92.96.143:5432 (外部)
Database: word_app
User: word_user
Password: word_pass_2024
```

## 应用访问信息

```
应用地址: http://47.92.96.143:3000
API地址: http://47.92.96.143:3000/api
```

## 注意事项

1. **数据库选择**: 最终选择PostgreSQL而非SQLite，因为应用使用了大量PostgreSQL特性
2. **构建时间**: Docker构建需要3-5分钟，请耐心等待
3. **数据导入**: 必须等待应用完全启动后再导入数据
4. **环境变量**: Vercel Blob Token已配置在.env.server中
5. **容器依赖**: 应用容器依赖PostgreSQL容器健康检查通过

## 常用运维命令

### 查看日志
```bash
# 查看应用日志
ssh root@47.92.96.143 "cd /root/word-app && docker compose logs app -f"

# 查看数据库日志
ssh root@47.92.96.143 "cd /root/word-app && docker compose logs postgres -f"

# 查看所有日志
ssh root@47.92.96.143 "cd /root/word-app && docker compose logs -f"
```

### 重启服务
```bash
# 重启应用容器
ssh root@47.92.96.143 "cd /root/word-app && docker compose restart app"

# 重启所有容器
ssh root@47.92.96.143 "cd /root/word-app && docker compose restart"
```

### 停止服务
```bash
# 停止所有容器
ssh root@47.92.96.143 "cd /root/word-app && docker compose stop"

# 停止并删除所有容器
ssh root@47.92.96.143 "cd /root/word-app && docker compose down"
```

### 数据库备份
```bash
# 备份数据库
ssh root@47.92.96.143 "docker exec postgres-word pg_dump -U word_user word_app > /root/word-app/backup-$(date +%Y%m%d).sql"
```

## 联系信息

- 服务器: 阿里云 ECS
- SSH: root@47.92.96.143
- 部署时间: 2025-12-10

---

**最后更新**: 2025-12-10 20:06
**状态**: ✅ 部署成功，应用正常运行
**应用地址**: http://47.92.96.143:3000
