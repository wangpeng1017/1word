# 🎯 使用SQL直接创建表

## 问题
Vercel无服务器环境不支持执行`npx`命令。

## 最终解决方案
使用Prisma的`$executeRawUnsafe`直接执行SQL创建表。

## 修改内容
1. ✅ 创建`lib/create-tables.ts` - 包含所有表的SQL创建语句
2. ✅ 更新`/api/setup` - 使用SQL直接创建表
3. ✅ 支持幂等操作 - 使用`CREATE TABLE IF NOT EXISTS`

## 立即部署

```bash
cd /e/trae/1单词

git add .
git commit -m "Fix: Use SQL to create tables instead of npx command"
git push origin main
```

## 部署完成后测试

1. **访问**：https://11word.vercel.app/setup

2. **点击"开始初始化"**

3. **等待完成**（约5-10秒）

4. **登录**：
   - 访问：https://11word.vercel.app/login
   - 邮箱：`admin@vocab.com`
   - 密码：`admin123456`

## 工作原理

1. 检测表是否存在
2. 如果不存在，执行SQL创建：
   - users 表
   - teachers 表
   - students 表
   - classes 表
   - vocabularies 表
   - 所有索引和外键约束
3. 创建默认管理员账号
4. 返回成功

## 优势

- ✅ 不依赖外部命令
- ✅ 在Vercel无服务器环境中可运行
- ✅ 幂等操作，可重复执行
- ✅ 创建所有必需的表和约束

---

**这次一定能成功！** 🚀
